CREATE TABLE GenreEntity (
  genreId INTEGER PRIMARY KEY NOT NULL,
  name TEXT NOT NULL
);

selectAll:
SELECT *
FROM GenreEntity;


insert:
INSERT INTO GenreEntity(genreId, name)
VALUES (?, ?);

insertFullGenreObject:
INSERT INTO GenreEntity(genreId, name)
VALUES ?;


deleteAll:
DELETE FROM GenreEntity;